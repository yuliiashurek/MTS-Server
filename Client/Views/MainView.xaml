<Window x:Class="Client.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localization="clr-namespace:Client.Localization"
        xmlns:properties="clr-namespace:Client.Properties"
        Title="Material Tracking System" Height="600" Width="1000"
        MinWidth="800" MinHeight="600"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 📋 Ліва панель -->
        <Border Grid.Column="0" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="1">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Company -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="⚙ MTS" FontSize="18" FontWeight="Bold"/>
                    <Rectangle Height="1" Fill="#AAA" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Основне меню -->
                <StackPanel Grid.Row="1" Margin="0,10,0,10">
                    <TextBlock FontSize="14" Margin="0,5">
                        <Run Text="📊 "/>
                        <Run Text="{x:Static properties:Resources.MainViewDashboardsMenu}"/>
                    </TextBlock>
                    <TextBlock FontSize="14" Margin="0,5">
                        <Run Text="📦 "/>
                        <Run Text="{x:Static properties:Resources.MainViewMaterialsMenu}"/>
                    </TextBlock>
                    <TextBlock FontSize="14" Margin="0,5">
                        <Run Text="📝 "/>
                        <Run Text="{x:Static properties:Resources.MainViewReportingMenu}"/>
                    </TextBlock>


                    <!-- Кастомний Expander -->
                    <StackPanel>
                        <Border Background="Transparent"
                                MouseLeftButtonDown="ToggleExpander_Click"
                                Cursor="Hand">
                            <DockPanel LastChildFill="False" Margin="0,5">
                                <TextBlock FontSize="14" VerticalAlignment="Center">
                                    <Run Text="📚 "/>
                                    <Run Text="{x:Static properties:Resources.MainViewDirectoriesMenu}"/>
                                </TextBlock>

                                <Path x:Name="ArrowIcon"
                                      Width="10" Height="10"
                                      Margin="5,0,0,0"
                                      Stretch="Uniform"
                                      Fill="Black"
                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                      VerticalAlignment="Center"
                                      DockPanel.Dock="Right">
                                    <Path.RenderTransform>
                                        <ScaleTransform x:Name="ArrowFlip" ScaleY="-1"/>
                                    </Path.RenderTransform>
                                </Path>
                            </DockPanel>
                        </Border>

                        <StackPanel x:Name="ReferencesPanel" Margin="15,5,0,0">
                            <TextBlock x:Name="SuppliersItem" Text="{x:Static properties:Resources.MainViewSuppliersMenu}"
                                       FontSize="13" Margin="0,4"
                                       Cursor="Hand"
                                       Foreground="#007ACC" FontWeight="Bold"
                                       MouseLeftButtonDown="Suppliers_Click"/>

                            <TextBlock x:Name="FormatsItem" Text="{x:Static properties:Resources.MainViewPrintFormatsMenu}"
                                       FontSize="13" Margin="0,4"
                                       Cursor="Hand"
                                       MouseLeftButtonDown="PrintFormats_Click"/>

                            <TextBlock x:Name="WarehousesItem" Text="{x:Static properties:Resources.MainViewWarehousesMenu}"
                                       FontSize="13" Margin="0,4"
                                       Cursor="Hand"
                                       MouseLeftButtonDown="Warehouses_Click"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Search + Help -->
                <StackPanel Grid.Row="2" Margin="0,20,0,10">
                    <TextBlock FontSize="14" Margin="0,5">
                        <Run Text="🔍 "/>
                        <Run Text="{x:Static properties:Resources.MainViewSearchMenu}"/>
                    </TextBlock>
                    <TextBlock FontSize="14" Margin="0,5">
                        <Run Text="❓ "/>
                        <Run Text="{x:Static properties:Resources.MainViewHelpMenu}"/>
                    </TextBlock>

                </StackPanel>

                <!-- Профіль -->
                <Border Grid.Row="3" Background="Black" CornerRadius="20" Margin="0,10,0,0">
                    <TextBlock FontSize="14" Foreground="White" Margin="10,4" HorizontalAlignment="Center">
                        <Run Text="👤 "/>
                        <Run Text="{x:Static properties:Resources.MainViewProfileMenu}"/>
                    </TextBlock>
                </Border>
            </Grid>
        </Border>

        <!-- 📄 Права частина -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Заголовок з мовою в одному рядку -->
            <DockPanel Grid.Row="0" Margin="10" LastChildFill="True">
                <!-- Заголовок -->
                <TextBlock x:Name="SectionTitle"
                           Text="Довідники"
                           FontSize="26"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Left"/>

                <!-- Комбо для мови -->
                <StackPanel Orientation="Horizontal"
            VerticalAlignment="Center"
            HorizontalAlignment="Right"
            DockPanel.Dock="Right"
            Margin="0,0,10,0">
                    <TextBlock Text="🌐"
               FontSize="16"
               VerticalAlignment="Center"
               Margin="0,0,5,0"/>
                    <ComboBox x:Name="LanguageSelector"
              Width="130"
              Height="30"
              VerticalContentAlignment="Center"
              SelectedValuePath="Tag"
              SelectionChanged="LanguageSelector_SelectionChanged">
                        <ComboBoxItem Content="English" Tag="en"/>
                        <ComboBoxItem Content="Українська" Tag="uk"/>
                    </ComboBox>
                </StackPanel>


            </DockPanel>

            <!-- Контент -->
            <ContentControl x:Name="MainContent" Grid.Row="1" Margin="0,10,0,10"/>
        </Grid>
    </Grid>
</Window>
